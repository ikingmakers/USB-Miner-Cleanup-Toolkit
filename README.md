# USB-Miner-Cleanup-Toolkit
PowerShell toolkit that scrubs an aggressive USB crypto miner which hijacks the Windows service layer and propagates through removable media. It targets shortcut-based malware families that replace real files with `.lnk` launchers pointing back to malicious payloads.
